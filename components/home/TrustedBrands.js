import Image from "next/image";
import { useBrands } from "@/hooks/useCatalogue";
export default function TrustedBrands(){const{brands,loading,error,retry}=useBrands("SMARTPHONE");return <section className="trusted-brands" aria-labelledby="trusted-brands-title"><div className="trusted-brands-container"><p className="section-label">Trusted Brands</p><span className="section-accent" aria-hidden="true"/><h2 id="trusted-brands-title">Shop Smartphones From Top Brands</h2><p className="trusted-brands-subheading">Explore the latest smartphones and accessories from leading global brands.</p>{loading?<State text="Loading trusted brands..."/>:error?<State text="Brands are temporarily unavailable." action="Try Again" onClick={retry}/>:<div className="brand-grid">{brands.map((brand)=><div className="brand-card" key={brand.id}><div className="brand-logo-frame">{brand.logoUrl?<Image className={`brand-logo brand-logo-${brand.slug}`} src={brand.logoUrl} alt={brand.name} width={140} height={70} sizes="140px"/>:<strong>{brand.name}</strong>}</div><span className="brand-name">{brand.name}</span></div>)}</div>}</div></section>}
function State({text,action,onClick}){return <div className="catalogue-home-state" role="status"><p>{text}</p>{action&&<button type="button" onClick={onClick}>{action}</button>}</div>}
