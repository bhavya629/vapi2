import styles from"@/styles/orderWorkflow.module.css";export const statusLabels={PENDING:"Pending",PENDING_PAYMENT:"Awaiting Payment",PENDING_CONFIRMATION:"Awaiting Store Confirmation",CONFIRMED:"Confirmed",PROCESSING:"Processing",PACKED:"Packed",READY_FOR_PICKUP:"Ready for Pickup",OUT_FOR_DELIVERY:"Out for Delivery",DELIVERED:"Delivered",CANCELLED:"Cancelled",FAILED:"Failed",REFUNDED:"Refunded"};export const label=v=>statusLabels[v]||String(v||"").replaceAll("_"," ").toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());export function OrderStatusBadge({value}){return <span className={`${styles.badge} ${styles[value?.toLowerCase()]||""}`}>{label(value)}</span>}export function PaymentStatusBadge({value}){return <span className={styles.payment}>{label(value)}</span>}export function FulfilmentBadge({value}){return <span className={styles.fulfilment}>{label(value)}</span>}
