import{useEffect,useRef}from"react";import{FiAlertTriangle,FiX}from"react-icons/fi";import styles from"@/styles/admin.module.css";export default function ConfirmDialog({title,message,confirmLabel="Confirm",onCancel,onConfirm,busy}){const cancel=useRef();useEffect(()=>{cancel.current?.focus();const key=e=>e.key==="Escape"&&onCancel();window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[onCancel]);return <div className={styles.dialogBack} onMouseDown={e=>e.target===e.currentTarget&&onCancel()}><section className={styles.dialog} role="dialog" aria-modal="true" aria-labelledby="dialog-title"><button className={styles.dialogClose} onClick={onCancel} aria-label="Close"><FiX/></button><FiAlertTriangle/><h2 id="dialog-title">{title}</h2><p>{message}</p><div><button ref={cancel} onClick={onCancel}>Cancel</button><button className={styles.danger} onClick={onConfirm} disabled={busy}>{busy?"Working...":confirmLabel}</button></div></section></div>}
