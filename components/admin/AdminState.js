import styles from"@/styles/admin.module.css";export default function AdminState({loading,error,empty,retry}){return <div className={styles.state} role="status">{loading&&<div className={styles.spinner}/>}<h2>{loading?"Loading admin data...":error?"We Could Not Load This Section":empty||"Nothing here yet"}</h2>{error&&<p>{error}</p>}{error&&<button onClick={retry}>Try Again</button>}</div>}
