import{allowMethods,requireCustomer,sendError}from"@/server/http/customerApi";import{PaymentError,verifyPayment}from"@/server/payments/paymentService";export default async function handler(req,res){if(!allowMethods(req,res,["POST"]))return;const user=await requireCustomer(req,res);if(!user)return;try{return res.json({success:true,data:await verifyPayment(user.id,req.body)})}catch(e){if(e instanceof PaymentError)return sendError(res,e.status,e.code,e.message);console.error("payment verify",e.code||e.message);return sendError(res,500,"INTERNAL_ERROR","Unable to verify payment.")}}
