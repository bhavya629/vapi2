import Head from"next/head";import Link from"next/link";import{useRouter}from"next/router";import{useEffect,useState}from"react";import Footer from"@/components/layout/Footer";import RazorpayButton from"@/components/payments/RazorpayButton";import{useAuth}from"@/context/AuthContext";import styles from"@/styles/orderRecords.module.css";const money=new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"});export default function Pending(){const router=useRouter(),{user,loading}=useAuth(),[payment,setPayment]=useState();useEffect(()=>{if(!loading&&!user)router.replace(`/login?redirect=${encodeURIComponent(router.asPath)}`)},[loading,user,router]);useEffect(()=>{if(user&&router.query.order)fetch(`/api/payments/order/${encodeURIComponent(router.query.order)}`).then(r=>r.json()).then(j=>setPayment(j.data))},[user,router.query.order]);return <><Head><title>Payment Pending | The Cellphone Studio</title><meta name="robots" content="noindex,nofollow"/></Head><main className={styles.page}><section className={styles.success}><span>!</span><p>PAYMENT PENDING</p><h1>{payment?.orderNumber||"Loading Payment"}</h1>{payment&&<><p>Your order is saved, but it is not confirmed until Razorpay payment is verified.</p><h2>{money.format(Number(payment.amount))}</h2><small>The inventory reservation is temporary. If it expires, contact the store or begin a new checkout.</small><div>{payment.canRetry&&<RazorpayButton orderNumber={payment.orderNumber}/>}<Link href={`/orders/${payment.orderNumber}`}>View Order</Link><Link href="/contact">Contact Store</Link></div></>}</section></main><Footer/></>}
